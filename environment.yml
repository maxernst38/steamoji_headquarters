name: vex-tracker
channels:
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pip
  - pip:
      - opencv-python
      - markdown            # renders the Learn guides in content/learn/
      - pyyaml              # the header block on each guide
